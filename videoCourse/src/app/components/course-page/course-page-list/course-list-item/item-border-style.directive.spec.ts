import { ItemBorderStyleDirective } from './item-border-style.directive';

describe('ItemBorderStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemBorderStyleDirective();
    expect(directive).toBeTruthy();
  });
});
